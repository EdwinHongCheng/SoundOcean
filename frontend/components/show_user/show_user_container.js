// [WIP] template for User Show Page

import { connect } from 'react-redux';
import ShowUser from './show_user';

const mSTP = state => {
    return {

    }
}

const mDTP = dispatch => {
    return {

    }
}

export default connect(mSTP, mDTP)(ShowUser);
